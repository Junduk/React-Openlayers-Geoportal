import React from "react";
import ReactDOM from "react-dom/client";
import DiplomskiContainer from "./DiplomskiContainer";
import "./index.css";

const root = ReactDOM.createRoot(document.getElementById("container"));
root.render(
  <DiplomskiContainer/>
);